tasks:
  - name: Install Package
    before: |
      npm i
      npm run build
      npm run add
  - name: Generate Junit Report
    before: |
      assertible-to-junit "$(cat ./example-jsons/single-test.json)" "$(cat ./example-jsons/tests.json)"
  - name: Service XUNIT Report
    before: |
      xunit-viewer -r test-report.xml -w -s -p 5050
